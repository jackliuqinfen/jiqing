# 合同建档备用路径验收记录

日期：2026-07-15

## 自动化结果

- 后端：`python -m unittest discover -s server/tests`，189 项通过。
- 前端逻辑：`node --test test/auditEligibility.test.ts test/documentReviewState.test.ts test/task4bLifecycleSafety.test.ts`，19 项通过。
- 生产构建：`npm.cmd run build` 通过；仅保留 Arco 依赖自身已有的 CSS 压缩警告。
- 代码格式：`git diff --check` 通过。

## 已覆盖场景

1. 系统识别原路径保持可用，失败任务可重试。
2. 已保存合同可主动进入手工复核，也可在系统识别失败后降级进入。
3. 已保存合同可粘贴外部 AI Markdown，经严格解析后进入统一复核。
4. 无合同文件只能保存当前用户草稿，不生成正式项目及生命周期事实。
5. 上传失败时仍保存已填草稿，并明确提示合同文件尚未上传成功。
6. 草稿支持原所有人继续填写、放弃和正式建档后完成归档。
7. 他人不能把未绑定合同挂到自己的草稿，查看者不能写入。
8. 重复请求复用幂等任务，降级原因不一致时拒绝冲突请求。
9. 外部 Markdown 的重复键、非法数字、未知字段、多个代码块和非法 JSON 会被拒绝。
10. 手工和外部 AI 字段保留真实来源，修改时记录对应的人工核对理由。
11. 备用任务不生成伪造 OCR 坐标、置信度或文本块。

## 待生产走查

- 使用一个真实账号和一份允许用于验收的真实合同，分别走查外部 AI 导入、无文件草稿续填、合同补传、逐项复核与正式创建。
- 确认迁移 `2026071501_contract_fallback` 和 `2026071502_contract_fallback_provenance` 已记录。
- 确认服务健康检查、页面加载和操作日志均正常。

生产走查前不写入任何示例项目或模拟财务数据。
